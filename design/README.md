# ollaic-ui

This is the React/Vite frontend used by the Tauri desktop app.

Run commands from the repository root, not from this directory:

```bash
pnpm install --frozen-lockfile
pnpm tauri:dev
```

See `../README.md` for the full setup, verification, and troubleshooting guide.
